# Example — copy to docs/rule-install-status.yaml when the first install decision is recorded.
# Live file: docs/rule-install-status.yaml (project-specific; commit to share with team)

version: 1

# One entry per tool. Status: installed | declined
# Do not ask again for a tool when status is installed or declined
# unless the user explicitly requests install/reinstall for that tool.
# Install/refresh: open docs/templates/agent/tools/<key>.md only.

tools:
  cursor:
    status: installed
    recorded: 2026-06-12
    path: .cursor/rules/modular-documentation.mdc

  grok-build:
    status: installed
    recorded: 2026-07-18
    path: AGENTS.md
    note: Documentation workflow section; doc roles under .grok/agents/

  github-copilot:
    status: declined
    recorded: 2026-06-12
    note: User installs manually on work machine

  # claude-code:
  #   status: installed
  #   recorded: 2026-06-12
  #   path: .claude/rules/modular-documentation.md

  # agents-md:
  #   status: installed
  #   recorded: 2026-06-12
  #   path: AGENTS.md
  #   note: Documentation workflow section only

  # continue:
  #   status: declined
  #   recorded: 2026-06-12

  # cline:
  #   status: installed
  #   recorded: 2026-06-12
  #   path: .clinerules/modular-documentation.md

# Optional pack rules (not the main modular-docs rule). Status: enabled | declined
# Bootstrap Step 4b / 4c records these; each tools/<key>.md installs matching artifacts.

optional_rules:
  template-update-check:
    status: enabled
    recorded: 2026-07-16
    # paths:
    #   cursor: .cursor/rules/template-update-check.mdc
    #   grok-build: AGENTS.md (Template update check section)

  doc-roles:
    status: enabled
    recorded: 2026-07-18
    # paths:
    #   cursor: .cursor/agents/
    #   grok-build: .grok/agents/
