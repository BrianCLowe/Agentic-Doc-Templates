# BlockEditor — Understanding

**Status**: `confirmed`

## What this is

The studio’s only editing engine. Block document model + save hooks. Role surfaces wrap it.

## What this is NOT

- NOT a UI shell
- NOT a second document format per role
